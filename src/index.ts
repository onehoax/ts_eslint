const favFruits: string[] = ["apple", "pear", "banana"];

function addFruit(fruit: string) {
    favFruits.push(fruit);
}

addFruit("oragne");

const myObj = {
    x: 1,
    y: 2
};

console.log(myObj);
